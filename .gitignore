# Binaries and build artifacts
bin/
build/
dist/
*.exe
*.exe~
*.dll
*.so
*.dylib
docker-dso
dso-cli
dso-provider-*
# ...but the above is meant for built plugin *binaries*, and it also matches
# the plugin source directories (cmd/plugins/dso-provider-<name>/), which made
# new source files there silently un-addable. The existing main.go files were
# only tracked because they predate the rule. Re-include the source dirs.
!cmd/plugins/dso-provider-*/
dso-test

# Test outputs
*_cov.out
coverage.out
coverage.log
*.out
race_output.log
test_output.log

# Profiling artifacts
*.prof
*.pprof

# Analysis/Progress Reports (use docs/ for persistent documentation)
ANALYSIS_*.md
ANALYSIS_SUMMARY.txt
IMPLEMENTATION_*.md
IMPLEMENTATION_SUMMARY.txt
PHASE_*.md
*_SUMMARY.*
CODE_VALIDATION_SUMMARY.md
COMPILATION_FIXES_SUMMARY.md
COMPREHENSIVE_ANALYSIS.md
docs/audit/COMPREHENSIVE_REVIEW.md
PRODUCTION_HARDENING_SUMMARY.txt

# Temporary/Patch files
patch.go
*.tmp
*.cache

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE/Editor files
.idea/
.vscode/
*.swp
*.swo
*~
.project
.settings/
.classpath

# Environment variables and secrets
.env
.env.*
secrets/

# Exception: migrate's fixture .env files are test data (non-secret,
# fabricated values), not real environment/secret files, and are required
# for internal/cli's migration test suite to pass on a fresh clone.
!internal/cli/testdata/migrate/**/.env

# Exception: the WebUI's /secrets route is a Next.js page directory (source
# code only -- it renders read-only secret *metadata* and never plaintext
# values, see web/components/secrets-content.tsx), not a real secrets
# directory. Without this it would be silently excluded by `secrets/` above.
!web/app/secrets/
test_local_secret
.claude
# Dependencies
vendor/
node_modules/

# Local testing and tools
ffmpeg-*
reports/
CLAUDE.md

# Local release utility — not committed, run manually
scripts/bump-version.sh

# Web UI build artifacts (source under web/ is tracked; see web/.gitignore
# for node_modules/.next/out exclusions within that tree)
web/node_modules/
web/.next/
web/out/
ui-dist/
docs/audit
.claude/
.claude*