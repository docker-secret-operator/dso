services:
  app:
    image: myapp:latest
    environment:
      DB_PASSWORD: ${DB_PASSWORD}
      DB_PASSWORD_DEFAULTED: ${DB_PASSWORD:-fallback}
      SOME_OTHER_INTERPOLATION: ${SOME_UNRELATED_HOST_VAR}
