## dso config show

Display current DSO configuration

### Synopsis

Show displays the current DSO configuration file.

The configuration file location is determined by:
1. CLI flag (-c/--config)
2. /etc/dso/dso.yaml (system-wide, readable by dso group)
3. ~/.dso/config.yaml (if in local mode)
4. ./dso.yaml (current directory)

Examples:
  docker dso config show              # Show default config
  docker dso config show -c custom.yaml  # Show specific config file

```
dso config show [flags]
```

### Options

```
  -h, --help   help for show
```

### Options inherited from parent commands

```
  -c, --config string   config file (searches: /etc/dso/dso.yaml, ./dso.yaml, dso.yaml) (default "dso.yaml")
```

### SEE ALSO

* [dso config](dso_config.md)	 - Manage DSO configuration

###### Auto generated by spf13/cobra on 19-May-2026
