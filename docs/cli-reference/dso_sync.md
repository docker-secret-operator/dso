## dso sync

Trigger immediate secret synchronization

### Synopsis

Force immediate secret reconciliation without waiting for watcher events.

This command connects to the running DSO agent and triggers an immediate
reconciliation cycle. Useful for:
- Testing configuration changes
- Emergency secret rotations
- Debugging synchronization issues
- Forcing updates without waiting for watcher

The agent must be running (via 'docker dso up' or systemd).

Examples:
  docker dso sync              # Sync all secrets
  docker dso sync --secret db_password  # Sync only specific secret
  docker dso sync --timeout 60s         # Custom timeout

```
dso sync [flags]
```

### Options

```
      --agent-socket string   Agent socket path (default "/run/dso/dso.sock")
  -h, --help                  help for sync
      --secret string         Only sync specific secret (optional)
      --timeout duration      Reconciliation timeout (default 30s)
```

### Options inherited from parent commands

```
  -c, --config string   config file (searches: /etc/dso/dso.yaml, ./dso.yaml, dso.yaml) (default "dso.yaml")
```

### SEE ALSO

* [dso](dso.md)	 - Docker Secret Operator (DSO) — Secret lifecycle runtime for Docker Compose

###### Auto generated by spf13/cobra on 18-May-2026
